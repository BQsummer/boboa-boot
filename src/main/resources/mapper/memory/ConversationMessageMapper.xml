<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bqsummer.mapper.memory.ConversationMessageMapper">
    
    <!-- 查询最近的消息 -->
    <select id="findRecentMessages" resultType="com.bqsummer.common.dto.memory.ConversationMessage">
        SELECT id, user_id, ai_character_id, session_id, sender_type, content, created_at, metadata
        FROM conversation_message
        WHERE user_id = #{userId}
          AND ai_character_id = #{aiCharacterId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    
</mapper>
